# SocialFTE Worker dependencies — Week 3
# FastAPI + ASGI server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Scheduling
apscheduler>=3.10.4

# Database
sqlalchemy>=2.0.23
asyncpg>=0.29.0
pgvector>=0.2.4

# HTTP client (for platform API calls)
httpx>=0.25.2

# Settings management
pydantic>=2.5.3
pydantic-settings>=2.1.0

# Storage (Cloudflare R2 / S3-compatible)
boto3>=1.33.8

# YouTube API
google-api-python-client>=2.110.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# Discord (ed25519 signature verification)
PyNaCl>=1.5.0

# Media processing
ffmpeg-python>=0.2.0

# Faster whisper (for audio transcription — Week 4)
# faster-whisper>=0.10.0  # Uncomment when needed

# LLM agents — OpenRouter via LiteLLM (Week 4). The [litellm] extra is required for
# the litellm/<provider>/<model> string-prefix form used in apps/worker/agents/base.py
# (research.md Decision 1 — no separate LiteLLM Proxy server, no litellm.yaml).
openai-agents[litellm]>=0.1.0

# Testing
pytest>=7.4.3
pytest-asyncio>=0.23.2
pytest-mock>=3.12.0
respx>=0.20.2  # httpx mocking

# Logging
python-json-logger>=2.0.7
